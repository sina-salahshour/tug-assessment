import { createTheme } from '@mui/material';

export const theme = createTheme({
	typography: {
		fontFamily: 'Inter, sans-serif',
	},
	colorSchemes: {
		light: {
			palette: {
				primary: {
					main: '#00AA4F',
				},
			},
		},
	},
});
